import React from "react";
import PersonalInfo from "./PersonalInfo";

const SideProfile = () => {
    return(
        <React.Fragment>
            <PersonalInfo/>
        </React.Fragment>
        );
};

export default SideProfile;